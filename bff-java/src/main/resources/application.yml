server:
  port: 8080

spring:
  application:
    name: risk-dashboard-bff
  data:
    redis:
      host: redis
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info

